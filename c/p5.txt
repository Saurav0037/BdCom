Macro & Typedef

g:

1. what is typedef? is it used for create a new type?
2. why do we need typedef?
3. pointers on c.pdf 14.2
4. The name of micro & typedef (programing specification)

Practice:

1. What is the difference between define and typedef? Illustration 
2. How to define a macro fuction? What are the points for attention?
3. Name two advantage of using define in place for literal constants.
4. What is the type of T1 (typedef struct foo *T1;)
5. create swap(t,x,y) macro which will swap x,y of type t
6. macro to find the smallest of 3
7. when typedef is needed.
8. difference of macro function and normal function.